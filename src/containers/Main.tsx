import * as React from "react";import {    Link,    Route,} from "react-router-dom";import Header from "../components/Header";import Film from "./FilmItem";import Films from "./Films";import Settings from "../components/Settings";import config from "../config";const styles = {    header: {        width: "100%",        height: "50px",    },};interface IState {    maximumCountGroupsOfGenres: number;}export default class Main extends React.Component<{}, IState> {    constructor(props: any) {        super(props);        this.state = {            maximumCountGroupsOfGenres: config.maximumCountGroupsOfGenres,        };    }    public render() {        return(            <div>                <div style={styles.header}>                    <Header/>                </div>                <Route exact path="/" key="main" render={ () => ( <Films                    maximumCountGroupsOfGenres={this.state.maximumCountGroupsOfGenres} /> )}/>                <Route path="/film/:filmId" key="film" component={Film}/>                <Route path="/settings" key="settings"                       render={ () => (<Settings value={this.state.maximumCountGroupsOfGenres}                                                 setSettings={this.setmaximumCountGroupsOfGenres.bind(this) } />) }/>            </div>        );    }    private setmaximumCountGroupsOfGenres(value: number) {        this.setState({ maximumCountGroupsOfGenres: value });    }}